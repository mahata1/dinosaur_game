/*
add moon(dark) and sun (bright background)
do commentary as user jumps add put audio effect
make game such that it asks for player name (if new) and switch to 
respective player if already exists
record the data such as user names their high scores, all scores, no. of games played
*/
